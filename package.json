{
  "name": "finalfrontend",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "serve -s client/build"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}